pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                sh 'echo "Oi para nosso Jenkins"'
                sh '''
                    echo "Nossa pipeline"
                    ls -la
                '''
            }
        }
    }
}
